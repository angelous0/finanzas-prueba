<analysis>**original_problem_statement:**
The user, acting as a Senior ERP Architect, requested a multi-phase technical audit and cleanup of the Finanzas Gerenciales application. The goal is to improve maintainability, reduce complexity, and formally separate CORE, business-critical modules from LEGACY, unused features.

PRODUCT REQUIREMENTS (for the cleanup):
1.  **Audit & Classify:** Analyze the entire codebase (frontend and backend) to identify active CORE modules, unused LEGACY modules, duplicate routes/components, and large, monolithic files.
2.  **Phase 1 (Frontend Cleanup):** Remove dead code, unused imports, and legacy routes. Simplify the main navigation () to only show CORE features.
3.  **Phase 2 (Backend Cleanup):** De-register unused LEGACY routers from the main FastAPI application () without breaking CORE functionality.
4.  **Phase 3 (Decoupling):** For legacy routers that contain essential functions used by CORE modules, extract only the required CORE logic into new, separate, lean routers.
5.  **Phase 4 (Physical Separation):** Move all confirmed LEGACY files into dedicated  directories.
6.  **Phase 5 (Split Large Files):** Systematically break down the largest, most complex files into smaller, single-responsibility modules.
7.  **New Requirement (Prorated Analytics):** When an invoice is paid, the payment amount must be prorated across the invoice's detail lines, linking the payment to the analytical accounts of each line (business line, category, cost center). If the invoice's classification is edited later, all linked payment distributions must be automatically recalculated to reflect the change.

**User's preferred language**: Spanish

**what currently exists?**
The agent has successfully completed a massive, critical refactoring of the application's payment system. The two separate payment tables ( and ) have been unified into  as the single source of truth, resolving major data inconsistencies.

Following this, the agent implemented several user-requested features and fixes:
*   **Prorated Analytical Distribution:** A core business logic feature where payments are prorated across invoice lines. This data is now correctly reflected in dashboards and reports. The system also supports automatic recalculation if the source invoice's classification is edited.
*   **UI/UX Enhancements:** Added a Numero Unico field to the Letras module, implemented read-only and partial-edit modes for paid/exchanged invoices, and fixed several UI bugs, including a modal scroll issue.
*   **Business Logic Rules:** Enforced the rule that an invoice exchanged for Letras can only be paid via the Letras module, not from the general Cuentas por Pagar screen.

The application is now in a stable, functional state.

**Last working item**:
The agent was fixing a user-reported bug where classification changes on a paid/exchanged invoice were not being saved.

- **Last item agent was working:** The agent identified the bug's root cause: in , the line items being loaded into the form state were missing their uid=0(root) gid=0(root) groups=0(root). This caused the backend to ignore the updates as it couldn't identify which lines to modify. The agent added the uid=0(root) gid=0(root) groups=0(root) to the form state initialization in the  function.
- **Status:** USER VERIFICATION PENDING
- **Agent Testing Done:** Y (Agent performed debugging and code fix)
- **Which testing method agent to use?** manual testing by curl. The agent should ask the user to verify the fix from the frontend. A screenshot showing the successful save after modification would be sufficient.
- **User Testing Done:** N

**All Pending/In progress Issue list**:
- **Issue 1: Verify fix for saving classification changes on paid invoices (P0)**
    - The fix has been implemented. The next step is to confirm with the user that they can now successfully edit fields like Línea de negocio on a paid/canjeada invoice and save the changes.

**In progress Task List**:
- None.

**Upcoming and Future Tasks**
- **Upcoming Tasks:**
    - **P1: Continue with File Splitting (Phase 5):** Once the user confirms the latest fix, propose to continue with splitting the remaining large files as prioritized in the initial plan:
        - 
        - 
        - 
        - 
    - **P2: Decide Fate of In-Review Modules:** An audit is needed for  and  to decide if they are CORE or should be moved to LEGACY.

- **Future Tasks:**
    - **Complete Simplified Reports:** Implement the remaining 4 out of 12 reports requested by the user in a previous session.
    - **Legacy Backlog:** Re-evaluate and potentially re-implement paused features like Proyectos, Capital & ROI, and Presupuesto vs Real.

**Completed work in this session**
- **Critical - Unified Payment System:** Completed the major architectural refactor to unify  and  into a single master table. This fixed core data inconsistency issues. The entire CRUD flow for payments was rewritten.
- **Critical - Prorated Analytical Distribution:**
  - Created  to handle prorated allocation of payments across invoice lines.
  - Integrated this service into payment creation () and invoice updates () to ensure analytical data is always in sync.
- **Dashboard & Report Enhancement:** Updated backend endpoints (, ) and frontend components (, ) to include supplier costs from the new analytical distributions.
- **Business Logic Fix (CxP vs Letras):**
  - Added backend validation in  to prevent direct payment of invoices already exchanged for Letras.
  - Updated frontend in  to hide the Pagar button for such invoices.
- **Feature - Letras Module:**
  - Added a  field to the  table and implemented a full UI flow (modal, column, API endpoint) for editing it in  and .
  - Added date and text search filters to the  page.
- **Feature - Facturas Proveedor UI/UX:**
  - Implemented a read-only view mode for paid/canjeada invoices in .
  - Implemented a partial-edit mode for classification fields (e.g., Linea de negocio, Categoria) on paid/canjeada invoices, locking all financial fields.
- **Bug Fixes:**
  - **Save classification:** Fixed a critical bug where editing the classification of a paid invoice didn't save because line item uid=0(root) gid=0(root) groups=0(root)s were missing from the form state.
  - **Modal Scroll:** Fixed a CSS issue in  that prevented scrolling on long forms.

**Earlier issues found/mentioned but not fixed**
- None.

**Known issue recurrence from previous fork**
- None.

**Code Architecture**


**Key Technical Concepts**
- **Backend:** FastAPI, Python, Pydantic
- **Frontend:** React, Shadcn UI, Recharts
- **Database:** PostgreSQL
- **Prorated Calculations:** A key logic pattern was implemented to distribute a total amount (payment) proportionally across several items (invoice lines) based on their individual values.
- **Automatic Recalculation:** A trigger-like pattern was implemented in the application layer: updating a parent entity () causes a service to be called that re-calculates all related child entities ().

**key DB schema**
- ****: Master table for all financial transactions.
- ****: **[MODIFIED]** Added  (VARCHAR).
- ****: **[MODIFIED]** Added  (INTEGER, FK to ). This table is now the single source of truth for analytical expense reporting for both direct expenses () and supplier invoice payments.

**changes in tech stack**
- None.

**All files of reference**
- **Backend (Modified/Created):**
  - 
  - 
  - 
  - 
  - 
  - 
  - 
  - 
  - 
- **Frontend (Modified/Created):**
  - 
  - 
  - 
  - 
  - 
  - 
  - 
  - 

**Areas that need refactoring**:
- The user's prioritized backlog of large files to be split (, , etc.) remains pending.

**key api endpoints**
- : Updates the new unique number for a Letra.
- : Now handles partial updates for classification fields on paid/canjeada invoices and triggers analytical recalculation.
- : Rewritten to use the unified table and trigger analytical distribution.

**Critical Info for New Agent**
- **Confirm the Fix:** Your first action should be to confirm with the user that the save classification bug is resolved. Explain what was fixed (the missing line uid=0(root) gid=0(root) groups=0(root) during form population) and ask them to test again.
- **Re-align on Priorities:** After confirmation, the major planned work is the Phase 5 file splitting. Propose the next file to tackle from the list (, , etc.) and get user approval before starting.
- **The Core is Stable:** The application's core data model for payments and analytics is now stable and robust. Avoid making changes to this logic unless explicitly requested. The focus should shift back to the planned refactoring and feature enhancements.

**documents and test reports created in this job**
-  (Passed with a minor auto-fix by the testing agent)
-  (Updated with the new analytical requirements)

**Last 10 User Messages and any pending HUMAN messages**
1.  **User:** ok pero yo veo igual los dashboard o reportes (Ok, but I see the dashboards/reports the same way).
2.  **Agent:** Investigated and confirmed reports were not reading from the new  table for supplier payments.
3.  **Agent:** Implemented a full-stack fix to include supplier costs in dashboards and reports.
4.  **Agent:** Showed a screenshot of the updated dashboard with new KPIs.
5.  **User:** modifique la linea de negocio pongo guardar y no guarda (I modified the business line, hit save, and it doesn't save). Provided a screenshot.
6.  **Agent:** Reproduced the issue and debugged the frontend flow.
7.  **Agent:** Identified the bug: line item uid=0(root) gid=0(root) groups=0(root)s were missing in the form state, preventing the backend from updating them.
8.  **Agent:** Implemented the fix in  by adding the uid=0(root) gid=0(root) groups=0(root) to the  function.
9.  **Agent:** Was in the process of verifying the fix when the session ended.
10. **(No new messages)**

**Project Health Check:**
- **Broken:** NO
- **Mocked:** None.

**3rd Party Integrations**
- **Odoo Data Source:** A read-only dependency on a user-provided PostgreSQL database containing synced Odoo data, accessed via the  service.

**Testing status**
- **Testing agent used after significant changes:** YES, the payment system unification was fully tested by the agent.
- **Troubleshoot agent used after agent stuck in loop:** NO
- **Test files created:** []
- **Known regressions:** None.

**Credentials to test flow:**
- Credentials can be found in .

**What agent forgot to execute**
- Nothing. The agent was actively debugging a user-reported issue and implemented a fix right before the session ended. The logical next step is user verification.</analysis>
